{{page-title @model.name}}
<BreadcrumbsItem as |linkClass|>
  <LinkTo
    data-test-project-name
    class={{linkClass}}
    @route="project"
    @model={{@model.id}}
  >
    {{@model.name}}
  </LinkTo>
</BreadcrumbsItem>
{{outlet}}