{{page-title @model.provider.name}}
<BreadcrumbsItem as |linkClass|>
  <LinkTo
    data-test-provider-name
    class={{linkClass}}
    @route="project.provider"
    @model={{@model.provider.id}}
  >
    Provider:
    {{@model.provider.name}}
  </LinkTo>
</BreadcrumbsItem>
{{outlet}}