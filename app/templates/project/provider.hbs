{{page-title @model.name}}
<BreadcrumbsItem as |linkClass|>
  <LinkTo
    data-test-provider-name
    class={{linkClass}}
    @route="project.provider"
    @model={{@model.name}}
  >
    Provider:
    {{@model.name}}
  </LinkTo>
</BreadcrumbsItem>
{{outlet}}