{{page-title @model.name}}
<BreadcrumbsItem as |linkClass|>
  <LinkTo
    data-test-api-name
    class={{linkClass}}
    @route="project.provider.api"
    @model={{@model.name}}
  >
    API:
    {{@model.name}}
  </LinkTo>
</BreadcrumbsItem>
<Methods @api={{@model}} />